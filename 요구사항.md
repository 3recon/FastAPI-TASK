## 1. 과제 목적

이번 과제는 **FastAPI를 이용하여 간단한 REST API 서버를 로컬 환경에서 실행**해 보고,
**JSON 파일 형태의 데이터를 읽고 수정하는 방법**을 실습하는 과제입니다.

학생들은 이번 과제를 통해 다음 내용을 경험하게 됩니다.

- FastAPI 서버 실행
- API 엔드포인트 구현
- JSON 파일 기반 데이터 처리
- GET / POST 요청 테스트
- Postman을 이용한 API 호출
- GitHub를 활용한 프로젝트 제출
- 데모 영상을 통한 동작 증빙

---

## 2. 과제 내용

이번 과제에서는 **수강기록(course records)** 이라는 JSON 데이터 구조를 다룹니다.

수강기록의 각 요소는 **하나의 이수 과목**을 의미하며,
각 과목은 아래 정보를 포함해야 합니다.

- 과목명
- 이수연도
- 이수학기
- 성적

예를 들어 하나의 수강기록은 아래와 같은 형태를 가질 수 있습니다.

```json
{
  "course_name": "오픈소스소프트웨어실습",
  "year": "2026",
  "semester": "1",
  "grade": "A+"
}
```

전체 데이터는 이러한 요소들이 들어 있는 **list 형태의 JSON 파일**로 관리합니다.

예시:

```json
[
  {
    "course_name": "자료구조",
    "year": "2025",
    "semester": "2",
    "grade": "A+"
  },
  {
    "course_name": "빅데이터프로그래밍",
    "year": "2025",
    "semester": "1",
    "grade": "A0"
  }
]
```

---

## 3. 구현해야 할 기능

### (1) GET `/courses`

Postman에서 **GET 요청**을 보내면,

현재 저장되어 있는 **전체 수강기록(JSON list 전체)** 이 리턴되어야 합니다.

즉, `/courses`에 GET 요청을 보냈을 때

JSON 파일에 저장된 모든 수강기록이 응답으로 반환되어야 합니다.

---

### (2) POST `/courses`

Postman에서 **POST 요청**을 보내면,

전송한 과목 정보가 기존 수강기록 list에 **새로운 요소로 추가**되어야 합니다.

즉, 수강기록의 list 안에 새로운 과목이 1개 더 늘어나는 방식으로 구현합니다.

예를 들어 아래와 같은 body를 전송하면,

```json
{
  "course_name": "인간로봇상호작용",
  "year": "2026",
  "semester": "2",
  "grade": "A+"
}
```

기존 JSON list의 마지막에 해당 과목 정보가 추가되어야 합니다.

---

## 4. 데이터 저장 방식

이번 과제에서는 수강기록 데이터를 **반드시 JSON 파일로 저장하고 관리**해야 합니다.

즉,

- Python 코드 안에 리스트를 하드코딩하는 방식은 금지합니다. 별도의 JSON 파일을 읽어와서 사용해야 합니다.
- POST 요청이 들어오면, 새로운 과목 정보를 JSON list에 추가한 뒤
    
    다시 파일에 반영하는 방식으로 구현합니다.
    

이번 과제에서는 JSON 파일 입출력 방식을 처음 다루는 학생이 있을 수 있으므로, 아래 **예시 코드**를 함께 남깁니다. courses.json 을 읽어와서, 1개 과목을 추가하고, 다시 덮어씌우는 명령어입니다. 

```python
import json

with open("courses.json", "r", encoding="utf-8") as f:
    data = json.load(f)

data.append({
	  "course_name": "인간로봇상호작용",
	  "year": "2026",
	  "semester": "2",
	  "grade": "A+"
})

with open("courses.json", "w", encoding="utf-8") as f:
    json.dump(data, f, ensure_ascii=False, indent=2)
```

---

## 5. 오류 처리 관련 조건

POST 요청 시 입력 형식이 일부 맞지 않거나 값이 기대 범위를 벗어나더라도,

**서버 앱이 강제 종료되면 안 됩니다.**

즉, 잘못된 요청이 들어오더라도:

- 서버가 꺼지지 않아야 하며
- 이후 요청도 계속 받을 수 있어야 합니다

오류 상황에서는 적절한 에러 응답을 반환하거나, FastAPI 기본 검증 응답이 나타나더라도 서버가 계속 실행되는 상태여야 합니다.

---

## 6. 실행 조건

이번 과제는 **로컬 환경에서 실행**하면 됩니다.

별도의 클라우드 배포는 요구하지 않습니다.

즉, 본인의 컴퓨터에서 FastAPI 서버를 실행하고, Postman으로 요청을 보내어 동작을 확인하면 됩니다.

---

## 7. 데모 영상 조건

데모 영상에서는 반드시 **Postman 호출 화면과 로컬 터미널 화면이 함께 보이도록** 해야 합니다.

### 영상에 반드시 포함되어야 하는 내용

- 로컬 터미널에서 FastAPI 서버가 실행 중인 장면
- Postman에서 `GET /courses` 호출
- 전체 수강기록이 반환되는 장면
- Postman에서 `POST /courses` 호출
- 새로운 과목이 추가되는 장면
- 다시 `GET /courses`를 호출하여 방금 추가한 과목이 실제로 반영된 것을 확인하는 장면
- File 을 열어서 추가한 과목 내역이 있는지 확인하는 장면

즉, 단순히 GET과 POST를 한 번씩 보여주는 것만으로는 부족하고,

**POST 이후 GET 재호출을 통해 데이터가 실제로 추가되었고, 파일에도 반영이 되었음을 보여주어야 합니다.**

---

## 8. 제출물

최종 제출물은 아래 두 가지입니다.

1. **GitHub repository 주소**
2. **데모 영상 링크 (조건은 실습 3과 동일)**

---

## 9. GitHub 제출 조건

프로젝트는 반드시 **GitHub 저장소 형태로 제출**해야 합니다. 매 실습마다 새로운 github repo 저장소로 관리해야합니다.

저장소에는 프로젝트 실행에 필요한 파일들이 포함되어 있어야 합니다.

예를 들어 아래와 같은 파일이 저장소 안에 포함될 수 있습니다.

- FastAPI 실행 코드 파일 (`main.py` 등)
- JSON 데이터 파일
- `requirements.txt`
- `.gitignore`

### 저장소 공개 방식

저장소는 기본적으로 **public repository**로 제출합니다.

다만, cheating이 우려되는 경우 제출 마감 전까지는 **private repository**로 작업할 수 있습니다.

단, **제출 마감 직후에는 반드시 public repository로 전환해야 합니다.**

---

## 10. 평가 기준

평가는 아래 요소를 종합적으로 반영합니다.

- `GET /courses`가 정상적으로 동작하는가
- `POST /courses`가 정상적으로 동작하는가
- POST 요청 시 새로운 과목이 JSON list에 추가되는가
- JSON 파일 기반으로 데이터를 관리하는가
- 잘못된 요청이 들어와도 서버가 강제 종료되지 않는가
- 데모 영상에서 터미널과 Postman이 함께 확인되는가
- POST 이후 GET 재호출로 반영 결과를 확인했는가
- GitHub 저장소가 정상적으로 제출되었는가
- 저장소 공개 조건을 지켰는가
- 프로젝트 구성이 적절히 정리되어 있는가

---

## 11. 참고

이번 과제의 핵심은 단순히 FastAPI 문법을 사용하는 것이 아니라,

**파일 기반 데이터를 읽고 수정하는 작은 API 서버를 직접 만들어 보는 것**입니다.

특히 다음 흐름을 이해하는 것이 중요합니다.

- JSON 파일 읽기
- GET 요청 시 전체 데이터 반환
- POST 요청 시 새 데이터 추가
- 변경된 내용을 다시 파일에 저장
- Postman으로 동작 확인

이 과제를 통해 다음 단계에서 더 다양한 CRUD API로 확장할 수 있는 기초를 익히게 됩니다.

<aside>
💡

**읽어두면 좋은 내용**

CRUD 가 여러 사용자에 의해 빈번하게 일어나면 file이 읽고 쓰여지는 업데이트 역시 자주 일어나게 됩니다. 이러한 부분을 최적화 된 것이 DATABASE 입니다. 2-2 또는 3-1에 배우게 될 거에요.

</aside>